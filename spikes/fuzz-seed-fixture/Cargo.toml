# SPDX-FileCopyrightText: 2026 Kevin Carlson
# SPDX-License-Identifier: Apache-2.0
#
# The subject `check-fuzz-seeds` checks. Outside the workspace: this crate
# exists to crash, and nothing that ships may depend on it.

[workspace]

[package]
name = "futhark-fuzz-seed-fixture"
version = "0.0.0"
description = "ADR-F060: a deliberately crashing parser, so check-fuzz-seeds has a live subject."
edition = "2024"
license = "Apache-2.0"
publish = false

[lints.rust]
unsafe_code = "forbid"
