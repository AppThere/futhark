{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Spike F1c main window. The app origin gets the bridge; the content origin must not.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
